# Archivum — PDF CMS

A single-container PDF document manager built with FastAPI + SQLite.  
Full CRUD: upload, view, edit metadata, delete PDFs — all in one Docker image.

## Docker
### Run
```bash
docker run -p 8000:8000 pdf-cms
# Visit http://localhost:8000


